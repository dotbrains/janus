# ==========================================
# Environment Configuration Example
# ==========================================
# This file shows example values for local development.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Update values in .env with your actual credentials
# 3. NEVER commit .env file to version control (it's in .gitignore)
# 
# NOTE: After running the initialization script, these values will be
# automatically updated with your project-specific names.
# ==========================================

# ==========================================
# Database Configuration
# ==========================================
DATABASE_URL=jdbc:postgresql://localhost:5432/janus
DATABASE_USERNAME=janus
DATABASE_PASSWORD=change-me-in-production
DATABASE_DRIVER=org.postgresql.Driver

# ==========================================
# Keycloak OAuth2 Configuration
# ==========================================
# NOTE: Default client secret is 'change-me' as configured in keycloak-realm.json
# Change this in production!
KEYCLOAK_CLIENT_ID=janus-client
KEYCLOAK_CLIENT_SECRET=change-me
KEYCLOAK_ISSUER_URI=http://localhost:8080/realms/janus
KEYCLOAK_AUTH_URI=http://localhost:8080/realms/janus/protocol/openid-connect/auth
KEYCLOAK_TOKEN_URI=http://localhost:8080/realms/janus/protocol/openid-connect/token
KEYCLOAK_USER_INFO_URI=http://localhost:8080/realms/janus/protocol/openid-connect/userinfo
KEYCLOAK_JWK_URI=http://localhost:8080/realms/janus/protocol/openid-connect/certs

# ==========================================
# Application Configuration
# ==========================================
SERVER_PORT=9090
JWT_ISSUER=http://localhost:9090

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# ==========================================
# Production Overrides (uncomment for production)
# ==========================================
# DATABASE_URL=jdbc:postgresql://production-db.example.com:5432/janus?ssl=true&sslmode=require
# DATABASE_USERNAME=prod_janus_user
# DATABASE_PASSWORD=super-secure-password-from-secrets-manager
# KEYCLOAK_ISSUER_URI=https://keycloak.example.com/realms/janus
# JWT_ISSUER=https://janus.example.com
# ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
